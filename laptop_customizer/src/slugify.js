import Slugify from 'slugify';

class Slugify extends React.Component {
    render() {
        return (
            <Slugify>
                <p>This is the slugify Component</p> 
            </Slugify>
        );
    }
}
export default Slugify;
